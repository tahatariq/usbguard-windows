# Core dependencies — versions pinned for security and reproducibility
# Run: pip install --require-hashes -r requirements.txt (when hash-pinned)
# To regenerate with hashes: pip-compile --generate-hashes requirements.in
fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.0
pydantic==2.8.2

# Development / testing only (not required for production)
# pytest==8.3.2
# pytest-asyncio==0.23.8
